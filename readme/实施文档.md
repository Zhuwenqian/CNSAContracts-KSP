# 实施文档

## 1. 安装方法
1. 确保已安装 KSP 1.8.1–1.12.x 版本。
2. 安装依赖模组：
   - ModuleManager
   - Contract Configurator
   - Real Solar System (RSS)
   - Realism Overhaul (RO)
   - RP-1（RP-0，提供 1950–1960 年代科技树节点）
   - KerwisChineseAerospacePack（提供 DFH-1、CZ-1 与 YF 发动机部件）
3. 将本仓库中的 `GameData/CNSAContracts/` 文件夹复制到 KSP 安装目录的 `GameData/` 下。
4. （可选）为机构准备标志贴图：
   - 256×160 尺寸：`GameData/CNSAContracts/Agencies/DFH1_Agency.png`
   - 64×40 尺寸：`GameData/CNSAContracts/Agencies/DFH1_Agency_scaled.png`
   - 若暂不提供贴图，KSP 将使用默认机构图标。
5. 启动游戏，ModuleManager 会自动处理 `GameData/CNSAContracts/Patches/CNSA_TechTree.cfg`，
   将中国航天部件移动到 1950–1960 年代科技节点。
6. 在主菜单按 `Alt+F10` 打开 Contract Configurator 调试窗口，确认合同无红色报错。

## 2. 游戏内效果
- 在 RP-1 环境下，本包补丁会将 DFH-1、CZ1_stage1、CZ1_stage2、CZ1_stage3、
  CZ1_coupling、CZ1_fairing、YF2_Engine、YF2_with_shell、YF3_Engine、
  YF3_with_shell 共 10 个部件的科技节点调整到 1950–1960 年代，降低解锁门槛。
- 游戏开始后，当玩家满足以下条件时，任务控制中心会提供“东方红一号卫星发射任务”：
  - 已解锁 DFH-1、CZ1_stage1、CZ1_stage2、CZ1_stage3、CZ1_coupling、CZ1_fairing 部件；
  - 已达到过太空（ReachSpace）；
  - 当前 RO 时间早于 1970 年 4 月 30 日。
- 合同要求：
  - 接受合同后发射一枚新火箭；
  - 火箭发射时必须同时包含 CZ-1 一级、二级、三级、级间段、整流罩以及 DFH-1 卫星部件；
  - 进入地球轨道，且近地点 ≥ 140 km、远地点 ≤ 2000 km。
- 合同截止时间显示为 1970 年 4 月 30 日；超过该日期后不再提供新合同。
- 因 `VesselParameterGroup` 设置 `completeInSequence = true`，CZ-1 各级部件在火箭刚发射、尚未分离时即被检测并锁定，入轨后分离掉的部件不会导致参数失败。

## 3. 常见问题与调整

### 3.1 起始年份不是 1951
若使用的 RP-1/RSS 配置将生涯起点设为其他年份，需重新计算 `targetDateSeconds`：
1. 计算从起点到 1970-04-30 的总天数（注意闰年）。
2. 总秒数 = 总天数 × 86400。
3. 修改 `GameData/CNSAContracts/Contracts/DFH1_Launch.cfg` 中 `DATA/targetDateSeconds` 的值。

### 3.2 不想强制使用 CZ-1 部件
可删除或注释掉以下节点：
- `REQUIREMENT` 中的 `PartUnlocked_CZ1_*`；
- `PARAMETER` 中的 `PartValidation_CZ1_*`。
但这样会削弱任务的历史还原度。

### 3.3 不想强制使用 DFH-1 部件
可删除或注释掉以下节点：
- `REQUIREMENT` 中的 `PartUnlocked_DFH1`；
- `PARAMETER` 中的 `PartValidation_DFH1`。

### 3.4 调整奖励
在 `DFH1_Launch.cfg` 中修改以下字段：
- `advanceFunds`：接受合同时预付款；
- `rewardFunds`：完成任务奖励资金；
- `rewardScience`：奖励科技点；
- `rewardReputation`：奖励声望。

### 3.5 部件在科技树中位置仍然过晚
确认已安装 RP-1（RP-0）与 ModuleManager，且 `GameData/CNSAContracts/Patches/CNSA_TechTree.cfg`
存在。可在 `Tech Tree/TreeYears.csv` 中查找想要的年份与节点名称，然后修改补丁中对应
`@TechRequired = 节点名称`。

### 3.6 没有安装 RP-1 时使用本包
`CNSA_TechTree.cfg` 使用 `:NEEDS[RP-0|RP-1]`，未安装 RP-1 时补丁不会生效，合同文件仍可正常
加载；但 Kerwis 部件将保留原始 stock 科技节点，解锁时间可能较晚。

## 4. 13 条测试用例与预期结果

| 编号 | 输入条件 | 预期结果 |
|---|---|---|
| 1 | 新存档，未解锁 DFH-1 或任一 CZ-1 部件，时间为 1951-01-01 | 合同不会提供（未满足 PartUnlocked） |
| 2 | RP-1 环境下，在 R&D 中研发 `basicRocketryRP0` 节点 | YF2_Engine、YF2_with_shell、CZ1_stage1 变为已解锁 |
| 3 | RP-1 环境下，在 R&D 中研发 `orbitalRocketry1956` 节点 | YF3_Engine、YF3_with_shell、CZ1_stage2、CZ1_fairing 变为已解锁 |
| 4 | RP-1 环境下，在 R&D 中研发 `solids1956` 与 `earlyAvionics` 节点 | CZ1_stage3 与 DFH-1 变为已解锁 |
| 5 | 已解锁全部所需部件，未到达太空，时间为 1960-01-01 | 合同不会提供（未满足 ReachSpace） |
| 6 | 已解锁全部所需部件，已到达太空，时间为 1969-01-01 | 合同可能提供；接受后 deadline 约为 1 年 119 天 |
| 7 | 已解锁全部所需部件，已到达太空，时间为 1970-05-01 | 合同不会提供（Expression 要求失败） |
| 8 | 合同已提供，玩家接受后发射未带 DFH-1 部件的新火箭 | 合同参数 PartValidation_DFH1 无法完成 |
| 9 | 合同已提供，玩家使用旧火箭发射 | 合同参数 NewVessel 无法完成 |
| 10 | 玩家使用 CZ-1 全套部件 + DFH-1 发射新火箭，进入 150 km × 500 km 地球轨道 | 合同完成 |
| 11 | 玩家使用 CZ-1 全套部件 + DFH-1 发射新火箭，进入 130 km × 500 km 地球轨道 | 近地点不足，Orbit 参数失败 |
| 12 | 玩家使用 CZ-1 全套部件 + DFH-1 发射新火箭，进入 150 km × 2100 km 地球轨道 | 远地点过高，Orbit 参数失败 |
| 13 | 玩家完成任务后 | 合同 maxCompletions=1，不再重复提供 |
