# 功能更新记录

## 2026-07-05
- 新增返回式卫星历史任务合同 `GameData/CNSAContracts/Contracts/FSW1_Launch.cfg`：
  - 合同名 `CNSA_FSW1_Launch`，归属机构“中华人民共和国国防部第五研究院”；
  - 要求使用长征二号（CZ-2）运载火箭，飞船上必须包含 CZ2_VEB-Adapter、CZ2_Fairing、
    CZ2_Adapter、CZ2_3700-Interstage、CZ2_Stage1tank、CZ2_Stage2tank 共 6 个部件；
  - 任务目标：新火箭发射 → 进入地球轨道（近地点 ≥ 140 km、远地点 ≤ 2000 km）→
    成功返回地球（LANDED 或 SPLASHED）；
  - 截止日期按 1975 年 12 月 1 日计算，`targetDateSeconds = 786240000.0`；
  - 使用 `VesselParameterGroup` + `completeInSequence = true`，确保分离前锁定
    CZ-2 各部件检测，避免入轨后分离导致参数失败。
- 扩展 RP-1 科技树补丁 `GameData/CNSAContracts/Patches/CNSA_TechTree.cfg`：
  - 新增 CZ-2 相关 11 个部件的 `@TechRequired` 配置；
  - 将 CZ2_Stage1tank、CZ2_Stage2tank、CZ2_3700-Interstage、CZ2_Adapter、
    CZ2_Fairing、YF20_Engine、YF21_Engine、YF21_with_shell_CZ-2、YF22_Engine、
    YF23_Engine 移动到 `orbitalRocketry1964`（1964 年）；
  - 将 CZ2_VEB-Adapter 控制舱移动到 `matureAvionics`（1964 年）；
  - 为上述部件添加 `%RP0conf = true` 与 `%RSSROConfig = true`。
- 更新 `readme/开发文档.md` 与 `readme/实施文档.md`，补充 FSW1 合同设计说明、
  依赖部件、安装后效果与测试用例。

## 2026-07-04
- 修复 `GameData/CNSAContracts/Contracts/DFH1_Launch.cfg` 中的解析错误：
  - `deadline` 与 `maxExpiry` 字段需要 `Single`（秒），原写法使用 `Duration()` 导致
    `DataStoreCastException: Cannot cast from Duration to Single`；
  - 改为 `deadline = @/targetDateSeconds - UniversalTime()` 与
    `maxExpiry = @/targetDateSeconds - UniversalTime()`，直接传入剩余秒数。
- 新增 RP-1 科技树补丁 `GameData/CNSAContracts/Patches/CNSA_TechTree.cfg`：
  - 为 DFH-1、CZ1_stage1、CZ1_stage2、CZ1_stage3、CZ1_coupling、CZ1_fairing、
    YF2_Engine、YF2_with_shell、YF3_Engine、YF3_with_shell 共 10 个部件配置
    `@TechRequired`，将其移动到 1950–1960 年代的 RP-1 科技节点；
  - 为上述部件添加 `%RP0conf = true` 与 `%RSSROConfig = true`，使 RP-1 系统正确识别。
- 更新 `GameData/CNSAContracts/Contracts/DFH1_Launch.cfg` 文件头，补充科技树补丁、
  YF-2/YF-3 发动机依赖说明以及 deadline 修正说明。
- 更新 `readme/开发文档.md` 与 `readme/实施文档.md`，补充科技树补丁与 deadline 修正
  相关说明、测试用例。

## 2026-07-04
- 新增 CNSA 中国航天历史任务合同包（Contract Pack），安装路径为 `GameData/CNSAContracts/`。
- 新增专属机构“中华人民共和国国防部第五研究院”，并设置 `NoRandomContractMentality` 防止其他随机合同占用该机构。
- 新增首个历史任务合同“东方红一号卫星发射任务”（CNSA_DFH1_Launch）：
  - 要求使用长征一号（CZ-1）运载火箭：CZ1_stage1、CZ1_stage2、CZ1_stage3、CZ1_coupling、CZ1_fairing；
  - 要求载荷为 DFH-1 卫星部件；
  - 要求新火箭发射；
  - 轨道要求：地球近地点 ≥ 140 km、远地点 ≤ 2000 km；
  - 截止时间：RO 时间 1970 年 4 月 30 日；
  - 需要先解锁 DFH-1 与全部 CZ-1 相关部件并达到过太空。
- 目录结构：
  - `GameData/CNSAContracts/Agencies/Agents.cfg`
  - `GameData/CNSAContracts/Groups.cfg`
  - `GameData/CNSAContracts/Contracts/DFH1_Launch.cfg`
